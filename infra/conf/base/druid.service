[Unit]
Description=Druid service cluster
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/apache-druid-26.0.0-bin/bin
ExecStart=sh -c "${cmd_druid}"

[Install]
WantedBy=multi-user.target